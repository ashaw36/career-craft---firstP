import{beforeEach,describe,expect,it,vi}from"vitest";import{renderPage}from"../../src/features/pages";import{bind as bindSkills}from"../../src/features/actions/skills-learning";import{bind as bindUrl}from"../../src/features/actions/job-url";import{TaskStore}from"../../src/shared/state/tasks";import type{SkillLearningActions,UrlActions,WorkspaceDataSource,WorkspaceSnapshot}from"../../src/api/data-source";
const snapshot:WorkspaceSnapshot={experiences:[],personas:[],jobs:[{id:"j1",title:"Role",company:"Co",status:"applied",score:70,matched:[],missing:[],updatedAt:"2026-07-21"}],resumes:[],skills:[{id:"root",name:"Engineering",level:"1",evidence:1,category:"technical"},{id:"child",name:"Rust",level:"2",evidence:2,category:"technical",parentId:"root",description:"Systems",resources:[{kind:"course",title:"Rust Book",source:"official",url:"https://example.com/rust",estimatedHours:5}]}],learning:[1,2,3].map(i=>({id:`path:item${i}`,pathId:"path",title:`Resource ${i}`,skill:"Rust",source:i===1?"official":"community",effort:"1h",status:"pending",resourceUrl:`https://example.com/${i}`})),providers:[]};
const source=()=>({load:vi.fn().mockResolvedValue(snapshot),searchSkills:vi.fn().mockResolvedValue(snapshot.skills),getSkillResources:vi.fn().mockResolvedValue(snapshot.skills[1]!.resources),openResource:vi.fn(),saveCustomSkill:vi.fn(),deleteCustomSkill:vi.fn(),simulateWhatIf:vi.fn().mockResolvedValue({delta:12}),updateLearning:vi.fn(),completeLearning:vi.fn().mockResolvedValue({id:"draft-1",version:1,status:"draft",title:"Edited result",organization:"学习计划",rawDescription:"Done",sourceLearningTitle:"Resource 1",completionNote:"Done"}),resolveLearningExperience:vi.fn()});const tick=()=>new Promise(r=>setTimeout(r,0));
describe("W4-B skills learning jobs UX",()=>{beforeEach(()=>{vi.restoreAllMocks();Object.defineProperty(navigator,"clipboard",{configurable:true,value:{writeText:vi.fn().mockResolvedValue(undefined)}})});
 it("renders hierarchy, three valid gap resources, source filter and job paper layout",()=>{const skills=renderPage("skills",snapshot),learning=renderPage("learning",snapshot),jobs=renderPage("jobs",snapshot);expect(skills).toContain("上级技能：root");expect(skills).toContain("data-skill-detail");expect((learning.match(/data-open-resource/g)??[])).toHaveLength(3);expect(learning).not.toContain("不足 3 条");expect(learning).toContain("data-learning-source");expect(jobs).toContain("岗位原文");expect(jobs).toContain("试算技能影响");expect(jobs).toContain("data-select-job");expect(jobs).not.toContain("当前状态时间点")});
 it("filters skills, learning sources and job statuses accessibly",()=>{const s=source();const root=document.createElement("div");root.innerHTML=renderPage("skills",snapshot)+renderPage("learning",snapshot)+renderPage("jobs",snapshot);bindSkills(root,s as unknown as WorkspaceDataSource&SkillLearningActions,new TaskStore(),vi.fn());const category=root.querySelector<HTMLSelectElement>("[data-skill-category]")!;category.value="business";category.dispatchEvent(new Event("change"));expect([...root.querySelectorAll<HTMLElement>("[data-skill-row]")].every(x=>x.hidden)).toBe(true);const sourceFilter=root.querySelector<HTMLSelectElement>("[data-learning-source]")!;sourceFilter.value="official";sourceFilter.dispatchEvent(new Event("change"));expect([...root.querySelectorAll<HTMLElement>("[data-learning-row]")].filter(x=>!x.hidden)).toHaveLength(1);const job=root.querySelector<HTMLSelectElement>("[data-job-filter]")!;job.value="rejected";job.dispatchEvent(new Event("change"));expect(root.querySelector<HTMLElement>("[data-job-row]")!.hidden).toBe(true)});
 it("recalculates what-if on input without persisting skills",async()=>{vi.useFakeTimers();const s=source(),root=document.createElement("div");root.innerHTML=renderPage("skills",snapshot);bindSkills(root,s as unknown as WorkspaceDataSource&SkillLearningActions,new TaskStore(),vi.fn());root.querySelector<HTMLButtonElement>("[data-what-if]")!.click();const form=root.querySelector<HTMLFormElement>("[data-what-if-form]")!;form.querySelector<HTMLInputElement>('[name="required"]')!.value="Rust,SQL";form.querySelector<HTMLInputElement>('[name="hypothetical"]')!.value="SQL";form.dispatchEvent(new Event("input",{bubbles:true}));await vi.advanceTimersByTimeAsync(200);expect(s.simulateWhatIf).toHaveBeenCalledWith(["Rust","SQL"],[],["SQL"]);expect(root.querySelector("[data-what-if-result]")?.textContent).toContain("12");expect(s.saveCustomSkill).not.toHaveBeenCalled();vi.useRealTimers()});
 it("copies and safely opens resources with toast feedback",async()=>{const s=source(),root=document.createElement("div");root.innerHTML=renderPage("learning",snapshot);bindSkills(root,s as unknown as WorkspaceDataSource&SkillLearningActions,new TaskStore(),vi.fn());root.querySelector<HTMLButtonElement>("[data-copy-resource]")!.click();await tick();expect(navigator.clipboard.writeText).toHaveBeenCalledWith("https://example.com/1");expect(root.querySelector("[data-toast]")?.textContent).toContain("已复制");root.querySelector<HTMLButtonElement>("[data-open-resource]")!.click();await tick();expect(s.openResource).toHaveBeenCalledWith("https://example.com/1")});
 it("exposes the same copy and safe-open actions in skill graph details",async()=>{const s=source(),root=document.createElement("div");root.innerHTML=renderPage("skills",snapshot);bindSkills(root,s as unknown as WorkspaceDataSource&SkillLearningActions,new TaskStore(),vi.fn());root.querySelector<HTMLButtonElement>('[data-skill-detail="child"]')!.click();await tick();const dialog=root.querySelector<HTMLElement>('[role="dialog"]')!;expect(dialog.textContent).toContain("Rust Book");dialog.querySelector<HTMLButtonElement>("[data-copy-resource]")!.click();await tick();expect(navigator.clipboard.writeText).toHaveBeenCalledWith("https://example.com/rust");dialog.querySelector<HTMLButtonElement>("[data-open-resource]")!.click();await tick();expect(s.openResource).toHaveBeenCalledWith("https://example.com/rust");expect(root.querySelector("[data-toast]")?.textContent).toContain("已安全打开")});
 it("requires confirmation before converting learning to experience",async()=>{const s=source(),root=document.createElement("div");root.innerHTML=renderPage("learning",snapshot);vi.stubGlobal("confirm",vi.fn().mockReturnValue(false));bindSkills(root,s as unknown as WorkspaceDataSource&SkillLearningActions,new TaskStore(),vi.fn());root.querySelector<HTMLButtonElement>("[data-complete-learning]")!.click();await tick();expect(s.completeLearning).not.toHaveBeenCalled()});
 it("opens detail buttons created by async search through delegation",async()=>{vi.useFakeTimers();const s=source(),root=document.createElement("div");root.innerHTML=renderPage("skills",snapshot);bindSkills(root,s as unknown as WorkspaceDataSource&SkillLearningActions,new TaskStore(),vi.fn());const input=root.querySelector<HTMLInputElement>("[data-skill-search]")!;input.value="Rust";input.dispatchEvent(new Event("input"));await vi.advanceTimersByTimeAsync(200);root.querySelector<HTMLButtonElement>('[data-skill-search-results] [data-skill-detail="child"]')!.click();await Promise.resolve();expect(root.querySelector('[role="dialog"] h1')?.textContent).toBe("Rust");vi.useRealTimers()});
 it("ignores a late what-if response",async()=>{vi.useFakeTimers();const pending:Array<(v:{delta:number})=>void>=[];const s=source();s.simulateWhatIf.mockImplementation(()=>new Promise(resolve=>pending.push(resolve)));const root=document.createElement("div");root.innerHTML=renderPage("skills",snapshot);bindSkills(root,s as unknown as WorkspaceDataSource&SkillLearningActions,new TaskStore(),vi.fn());root.querySelector<HTMLButtonElement>("[data-what-if]")!.click();const form=root.querySelector<HTMLFormElement>("[data-what-if-form]")!,field=form.querySelector<HTMLInputElement>('[name="hypothetical"]')!;field.value="old";form.dispatchEvent(new Event("input"));await vi.advanceTimersByTimeAsync(160);field.value="new";form.dispatchEvent(new Event("input"));await vi.advanceTimersByTimeAsync(160);pending[1]!({delta:22});await Promise.resolve();pending[0]!({delta:1});await Promise.resolve();expect(root.querySelector("[data-what-if-result]")?.textContent).toContain("22");vi.useRealTimers()});
 it("submits the editable learning draft only after confirmation and keeps it on failure",async()=>{const s=source(),root=document.createElement("div");root.innerHTML=renderPage("learning",snapshot);vi.stubGlobal("confirm",vi.fn().mockReturnValue(true));s.completeLearning.mockRejectedValueOnce(new Error("write failed"));bindSkills(root,s as unknown as WorkspaceDataSource&SkillLearningActions,new TaskStore(),vi.fn());root.querySelector<HTMLButtonElement>("[data-complete-learning]")!.click();const form=root.querySelector<HTMLFormElement>("[data-learning-draft] form")!;form.querySelector<HTMLInputElement>('[name="title"]')!.value="Edited result";form.dispatchEvent(new Event("submit",{cancelable:true}));await tick();expect(s.completeLearning).toHaveBeenCalledWith("path:item1",expect.objectContaining({title:"Edited result"}));expect(root.querySelector("[data-learning-draft]")).not.toBeNull();expect(form.querySelector<HTMLButtonElement>("button.primary")!.disabled).toBe(false)});
 it("keeps the returned experience as pending until explicit confirm or discard",async()=>{const s=source(),root=document.createElement("div");root.innerHTML=renderPage("learning",snapshot);bindSkills(root,s as unknown as WorkspaceDataSource&SkillLearningActions,new TaskStore(),vi.fn());root.querySelector<HTMLButtonElement>("[data-complete-learning]")!.click();root.querySelector<HTMLFormElement>("[data-learning-draft] form")!.dispatchEvent(new Event("submit",{cancelable:true}));await tick();expect(root.querySelector("[data-learning-draft]")?.textContent).toContain("待确认");expect(s.resolveLearningExperience).not.toHaveBeenCalled();root.querySelector<HTMLButtonElement>("[data-confirm-learning-experience]")!.click();await tick();expect(s.resolveLearningExperience).toHaveBeenCalledWith(expect.objectContaining({id:"draft-1",status:"draft"}),"confirmed")});
 it("keeps manual paste available when URL collection needs login",async()=>{const root=document.createElement("div");root.innerHTML='<form data-job-form><textarea name="jdText"></textarea></form>';const s={collectJobUrl:vi.fn().mockResolvedValue({manualFallbackRequired:true,reason:"LoginRequired"})};bindUrl(root,s as unknown as WorkspaceDataSource&UrlActions,new TaskStore());root.querySelector<HTMLInputElement>('input[type="url"]')!.value="https://example.com/job";root.querySelector<HTMLButtonElement>('button[type="button"]')!.click();await tick();expect(root.querySelector('[role="status"]')?.textContent).toContain("手动粘贴");expect(root.querySelector<HTMLTextAreaElement>("textarea")).not.toBeNull()});
});
